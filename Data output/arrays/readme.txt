The full array contains (transposed):

years,mcf_opt,ch4_opt,d13c_opt,fme_opt,fsl_opt,fst_opt,fch4_opt,ed13c_opt

for each year.

The xopt array contains the optimized state, which should in principle be able to reproduce
all results.
